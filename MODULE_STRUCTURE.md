# Module Structure Visualization

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         SOULACE V2                              │
│                   Mental Health Support Platform                │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │
              ┌───────────────┴───────────────┐
              │                               │
              ▼                               ▼
        ┌──────────┐                   ┌──────────┐
        │ FRONTEND │                   │ BACKEND  │
        │  (React) │◄─────────────────►│ (Node.js)│
        └──────────┘      REST API     └──────────┘
              │                               │
              │                               │
              ▼                               ▼
    ┌─────────────────┐              ┌──────────────┐
    │  3 Auth Modules │              │   MongoDB    │
    └─────────────────┘              └──────────────┘
```

---

## Frontend Module Architecture

```
┌───────────────────────────────────────────────────────────────────┐
│                        FRONTEND APPLICATION                        │
├───────────────────────────────────────────────────────────────────┤
│                                                                    │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │                    App.tsx (Router)                         │  │
│  │                                                             │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │  │
│  │  │ User Routes  │  │ Prof Routes  │  │ Admin Routes │    │  │
│  │  └──────────────┘  └──────────────┘  └──────────────┘    │  │
│  └────────────────────────────────────────────────────────────┘  │
│                                                                    │
│  ┌──────────────────┐ ┌──────────────────┐ ┌─────────────────┐  │
│  │   USER MODULE    │ │ PROFESSIONAL     │ │  ADMIN MODULE   │  │
│  │                  │ │    MODULE        │ │                 │  │
│  │ ┌──────────────┐ │ │ ┌──────────────┐ │ │ ┌─────────────┐│  │
│  │ │Store         │ │ │ │Store         │ │ │ │Store        ││  │
│  │ │userAuthStore │ │ │ │professionalAuth│ │ │ │adminAuthStore││  │
│  │ └──────────────┘ │ │ └──────────────┘ │ │ └─────────────┘│  │
│  │                  │ │                  │ │                 │  │
│  │ ┌──────────────┐ │ │ ┌──────────────┐ │ │ ┌─────────────┐│  │
│  │ │Pages:        │ │ │ │Pages:        │ │ │ │Pages:       ││  │
│  │ │- Login       │ │ │ │- Login       │ │ │ │- Login      ││  │
│  │ │- Signup      │ │ │ │- Signup      │ │ │ │- Dashboard  ││  │
│  │ │- Dashboard   │ │ │ │- Dashboard   │ │ │ │             ││  │
│  │ └──────────────┘ │ │ └──────────────┘ │ │ └─────────────┘│  │
│  │                  │ │                  │ │                 │  │
│  │ ┌──────────────┐ │ │ ┌──────────────┐ │ │ ┌─────────────┐│  │
│  │ │Components:   │ │ │ │Components:   │ │ │ │Components:  ││  │
│  │ │- Protected   │ │ │ │- Protected   │ │ │ │- Protected  ││  │
│  │ │  Route       │ │ │ │  Route       │ │ │ │  Route      ││  │
│  │ └──────────────┘ │ │ └──────────────┘ │ │ └─────────────┘│  │
│  └──────────────────┘ └──────────────────┘ └─────────────────┘  │
│                                                                    │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │                   Shared Resources                          │  │
│  │  - api.config.ts (API endpoints)                           │  │
│  │  - axios.ts (HTTP client)                                  │  │
│  │  - auth.types.ts (Type definitions)                        │  │
│  └────────────────────────────────────────────────────────────┘  │
└───────────────────────────────────────────────────────────────────┘
```

---

## Backend API Structure

```
┌────────────────────────────────────────────────────────────────┐
│                      BACKEND APPLICATION                        │
├────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │                    index.js (Main)                        │ │
│  │  - Express Server                                         │ │
│  │  - CORS Configuration                                     │ │
│  │  - Cookie Parser                                          │ │
│  │  - MongoDB Connection                                     │ │
│  └──────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐  │
│  │ User Routes    │  │ Professional   │  │ Admin Routes   │  │
│  │                │  │    Routes      │  │                │  │
│  │ /api/users     │  │ /api/          │  │ /api/admin     │  │
│  │                │  │  professional  │  │                │  │
│  │ ┌────────────┐ │  │ ┌────────────┐ │  │ ┌────────────┐ │  │
│  │ │ POST       │ │  │ │ POST       │ │  │ │ POST       │ │  │
│  │ │ /signup    │ │  │ │ /signup    │ │  │ │ /login     │ │  │
│  │ │ /login     │ │  │ │ /login     │ │  │ │ /logout    │ │  │
│  │ │ /logout    │ │  │ │ /logout    │ │  │ │            │ │  │
│  │ │            │ │  │ │            │ │  │ │ GET        │ │  │
│  │ │ GET        │ │  │ │ POST       │ │  │ │ /dashboard │ │  │
│  │ │ /checkAuth │ │  │ │ /upload-doc│ │  │ │            │ │  │
│  │ │            │ │  │ │            │ │  │ │ POST       │ │  │
│  │ │            │ │  │ │ GET        │ │  │ │ /insert    │ │  │
│  │ │            │ │  │ │ /verified  │ │  │ │            │ │  │
│  │ │            │ │  │ │ /unverified│ │  │ │            │ │  │
│  │ │            │ │  │ │            │ │  │ │            │ │  │
│  │ │            │ │  │ │ PUT        │ │  │ │            │ │  │
│  │ │            │ │  │ │ /update/:id│ │  │ │            │ │  │
│  │ │            │ │  │ │            │ │  │ │            │ │  │
│  │ │            │ │  │ │ DELETE     │ │  │ │            │ │  │
│  │ │            │ │  │ │ /delete/:id│ │  │ │            │ │  │
│  │ └────────────┘ │  │ └────────────┘ │  │ └────────────┘ │  │
│  └────────────────┘  └────────────────┘  └────────────────┘  │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │                    Middleware                             │ │
│  │  - authUser.middleware.js (User auth)                    │ │
│  │  - isAdmin.middleware.js (Admin auth)                    │ │
│  │  - rateLimiter.middleware.js (Rate limiting)             │ │
│  │  - multer.js (File upload)                               │ │
│  └──────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │                    Controllers                            │ │
│  │  - user.controller.js                                    │ │
│  │  - professional.controller.js                            │ │
│  │  - admin.controller.js                                   │ │
│  └──────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │                    Models                                 │ │
│  │  - User.model.js                                         │ │
│  │  - Professional.model.js                                 │ │
│  │  - Admin.model.js                                        │ │
│  └──────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────┘
```

---

## Authentication Flow

### User Authentication Flow

```
┌─────────┐      ┌─────────┐      ┌──────────┐      ┌──────────┐
│ Browser │      │Frontend │      │ Backend  │      │ MongoDB  │
└────┬────┘      └────┬────┘      └────┬─────┘      └────┬─────┘
     │                │                │                  │
     │  1. Navigate   │                │                  │
     ├───────────────►│                │                  │
     │  /user/login   │                │                  │
     │                │                │                  │
     │  2. Enter      │                │                  │
     │  Credentials   │                │                  │
     ├───────────────►│                │                  │
     │                │                │                  │
     │                │  3. POST       │                  │
     │                │  /api/users/   │                  │
     │                │     login      │                  │
     │                ├───────────────►│                  │
     │                │                │  4. Validate     │
     │                │                │  Credentials     │
     │                │                ├─────────────────►│
     │                │                │                  │
     │                │                │  5. Return User  │
     │                │                │◄─────────────────┤
     │                │  6. Set Cookie │                  │
     │                │  Return User   │                  │
     │                │◄───────────────┤                  │
     │                │                │                  │
     │  7. Store in   │                │                  │
     │  userAuthStore │                │                  │
     │◄───────────────┤                │                  │
     │                │                │                  │
     │  8. Redirect   │                │                  │
     │  /user/        │                │                  │
     │   dashboard    │                │                  │
     │◄───────────────┤                │                  │
     │                │                │                  │
```

### Professional Authentication Flow

```
Similar flow but uses:
- /professional/login endpoint
- professionalAuthStore
- /professional/dashboard redirect
- Professional-specific validation
```

### Admin Authentication Flow

```
Similar flow but uses:
- /admin/login endpoint
- adminAuthStore
- /admin/dashboard redirect
- Admin-specific authorization
```

---

## Data Flow - User Dashboard

```
┌──────────────┐
│    User      │
│  Dashboard   │
└──────┬───────┘
       │
       │ On Mount
       │
       ▼
┌──────────────────┐
│ UserProtected    │
│     Route        │
└──────┬───────────┘
       │
       │ Check Auth
       │
       ▼
┌──────────────────┐
│ userAuthStore    │
│ isAuthenticated? │
└──────┬───────────┘
       │
       ├─── No ───► Redirect to /user/login
       │
       └─── Yes ──► Continue
                    │
                    ▼
            ┌───────────────┐
            │ Load User Data│
            │ from Store    │
            └───────┬───────┘
                    │
                    ▼
            ┌───────────────┐
            │ Render        │
            │ Dashboard     │
            │ - Stats       │
            │ - Profile     │
            │ - Actions     │
            └───────────────┘
```

---

## Storage Architecture

### Browser Local Storage

```
localStorage
├── user-auth-storage
│   ├── state
│   │   ├── user: { _id, firstName, lastName, email, ... }
│   │   └── isAuthenticated: true/false
│   └── version: 0
│
├── professional-auth-storage
│   ├── state
│   │   ├── professional: { _id, firstName, lastName, ... }
│   │   └── isAuthenticated: true/false
│   └── version: 0
│
└── admin-auth-storage
    ├── state
    │   ├── admin: { _id, username, email, ... }
    │   └── isAuthenticated: true/false
    └── version: 0
```

### Backend Session Storage

```
Cookies (httpOnly, secure)
├── user_token (JWT for users)
├── professional_token (JWT for professionals)
└── admin_token (JWT for admins)
```

---

## File Structure Tree

```
Soulace-V2/
│
├── frontend/
│   ├── src/
│   │   ├── modules/
│   │   │   ├── user/
│   │   │   │   ├── store/
│   │   │   │   │   └── userAuthStore.ts
│   │   │   │   ├── pages/
│   │   │   │   │   ├── UserLogin.tsx
│   │   │   │   │   ├── UserSignup.tsx
│   │   │   │   │   └── UserDashboard.tsx
│   │   │   │   └── components/
│   │   │   │       └── UserProtectedRoute.tsx
│   │   │   │
│   │   │   ├── professional/
│   │   │   │   ├── store/
│   │   │   │   │   └── professionalAuthStore.ts
│   │   │   │   ├── pages/
│   │   │   │   │   ├── ProfessionalLogin.tsx
│   │   │   │   │   ├── ProfessionalSignup.tsx
│   │   │   │   │   └── ProfessionalDashboard.tsx
│   │   │   │   └── components/
│   │   │   │       └── ProfessionalProtectedRoute.tsx
│   │   │   │
│   │   │   └── admin/
│   │   │       ├── store/
│   │   │       │   └── adminAuthStore.ts
│   │   │       ├── pages/
│   │   │       │   ├── AdminLogin.tsx
│   │   │       │   └── AdminDashboard.tsx
│   │   │       └── components/
│   │   │           └── AdminProtectedRoute.tsx
│   │   │
│   │   ├── config/
│   │   │   └── api.config.ts
│   │   │
│   │   ├── App.tsx
│   │   └── pages/
│   │       └── Homepage.tsx
│   │
│   └── package.json
│
├── backend/
│   ├── src/
│   │   ├── routes/
│   │   │   ├── user.routes.js
│   │   │   ├── professional.route.js
│   │   │   └── admin.route.js
│   │   │
│   │   ├── controllers/
│   │   │   ├── user.controller.js
│   │   │   ├── professional.controller.js
│   │   │   └── admin.controller.js
│   │   │
│   │   ├── middleware/
│   │   │   ├── authUser.middleware.js
│   │   │   ├── isAdmin.middleware.js
│   │   │   └── rateLimiter.middleware.js
│   │   │
│   │   └── models/
│   │       ├── User.model.js
│   │       ├── Professional.model.js
│   │       └── Admin.model.js
│   │
│   ├── index.js
│   └── package.json
│
├── AUTHENTICATION_MODULES.md
├── QUICK_START_MODULES.md
└── MODULE_STRUCTURE.md
```

---

## URL Routes Map

```
Homepage Routes:
/                           → Homepage (Portal Selection)

User Module Routes:
/user/login                 → User Login Page
/user/signup                → User Signup Page
/user/dashboard            → User Dashboard (Protected)

Professional Module Routes:
/professional/login         → Professional Login Page
/professional/signup        → Professional Signup Page
/professional/dashboard    → Professional Dashboard (Protected)

Admin Module Routes:
/admin/login               → Admin Login Page
/admin/dashboard           → Admin Dashboard (Protected)

Legacy Routes (for compatibility):
/login                     → Old Login Page
/signup                    → Old Signup Page
/dashboard                 → Old Dashboard (Protected)
```

---

## Color Coding

```
┌────────────────────────────────────────────────┐
│ Module Color Themes                            │
├────────────────────────────────────────────────┤
│                                                │
│ 🔵 User Module                                 │
│    Primary: Blue (#3B82F6)                    │
│    Secondary: Indigo (#6366F1)                │
│    Gradient: from-blue-500 to-indigo-600      │
│                                                │
│ 🟢 Professional Module                         │
│    Primary: Teal (#14B8A6)                    │
│    Secondary: Green (#10B981)                 │
│    Gradient: from-teal-500 to-green-600       │
│                                                │
│ ⚫ Admin Module                                │
│    Primary: Gray-900 (#111827)                │
│    Secondary: Gray-800 (#1F2937)              │
│    Gradient: from-gray-800 to-gray-900        │
│                                                │
└────────────────────────────────────────────────┘
```

---

This visual guide provides a clear overview of how the three authentication modules are structured and interact with each other.

